{
  "name": "kiwoom-mcp-root",
  "version": "1.0.0",
  "description": "키움증권 OpenAPI REST API 연동 MCP 서버",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "start": "pnpm -r run start"
  },
  "keywords": [
    "kiwoom",
    "openapi",
    "mcp",
    "rest-api"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=7.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
